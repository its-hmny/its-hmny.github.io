# Sample workflow for building and deploying a Next.js app to GitHub Pages

name: Deploy to GitHub Pages

on:
  push: # Runs on pushes targeting the 'main' branch
    branches: ['main']
  workflow_dispatch: # Allows you to be run manually

permissions: # Sets minimal granular permissions of the GITHUB_TOKEN to allow deployment
  pages: write
  contents: read
  id-token: write

concurrency: # Allow one concurrent deployment
  group: 'pages'
  cancel-in-progress: true

jobs: 
  deploy:
    runs-on: ubuntu-latest
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v1

      - name: Install dependencies
        run: yarn install
      
      - name: Build with Next.js
        run: yarn build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./out

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v3